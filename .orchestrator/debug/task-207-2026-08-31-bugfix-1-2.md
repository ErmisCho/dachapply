# Debug session: TASK-207 focused frontend fixture ordering
Created: 2026-08-31T09:31:00Z
Session: task-207-2026-08-31-bugfix-1

## Phase 1 — Root Cause

### Error

```text
FAIL src/mailboxCollapse.test.tsx > TASK-177 a collapsed conversation message > distinguishes a captured stale app draft from the real sent owner message (TASK-207)
AssertionError: expected '<div>...' to contain 'STALE CONVERSATION REPLY'
...
Test Files 1 failed | 1 passed
Tests 1 failed | 16 passed
exit code 1
```

### Reproduction

```bash
cd frontend
npm test -- --run src/appPanels.test.tsx src/mailboxCollapse.test.tsx
```

Environment: Windows 11 / MINGW64, Node v26.3.1, npm 11.17.0. Frequency: 1/1.

### Suspect commits

- Current uncommitted TASK-207 test fixture — first introduction of a three-row synthetic app-draft thread.
- `TASK-134` chronological conversation work — `MailboxThreadGroup` deliberately expects the endpoint's newest-first input and reverses it for display.

### Instrumentation data

The failure's rendered markup contains `UNSENT DRAFT` and `STALE REPLY`, proving the row discriminator rendered. It has `aria-expanded="false"` for draft row id 3 and `aria-expanded="true"` for received row id 1. The test supplied `[oldest, owner, draft]`, while the real `/jobs/{id}/mailbox/` endpoint supplies newest-first and `chronologicalMessages` reverses that input. The draft block containing the longer stale text and controls only renders for the expanded last display row.

### Hypothesized root cause

The synthetic fixture used oldest-first order even though `MailboxThreadGroup`'s wire contract is newest-first, so the wrong row was expanded and the assertion looked for expanded draft controls in a collapsed draft. · Confidence: high

## Phase 2 — Pattern

Fixture/wire-order mismatch, not a product defect. Existing thread fixtures assert counts only and did not expose the order assumption.

## Phase 3 — Impact

Only the new TASK-207 test fixture ordering is wrong. Production endpoint ordering and chronological rendering must not change.

## Phase 4 — Solution

Supply the synthetic messages in the endpoint's newest-first order, then rerun both focused frontend files.

## Resolution

Corrected the synthetic fixture to the endpoint's newest-first order; both focused frontend files then reached the intended expanded draft row.
