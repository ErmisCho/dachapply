# Debug session: newly disclosed frontend transitive vulnerabilities
Created: 2026-09-01T20:21:00Z
Session: task-211-2026-09-01-bugfix

## Phase 1 — Root Cause

### Error

`npm audit --audit-level=high` exited 1 after tests/build passed:

- `browserslist <=4.28.6` — high — GHSA-c83g-rgw3-j3cx and GHSA-73wf-gq98-2v4g.
- `postcss-selector-parser >=6.1.0 <6.1.3` — low — GHSA-w9m9-85wc-3x92.
- Summary: `2 vulnerabilities (1 low, 1 high)`.

### Reproduction

`cd frontend && npm ci && npm audit --audit-level=high`

Environment: Node v26.3.1, npm 11.17.0, clean lockfile install. Frequency: always against the current npm advisory database.

### Suspect commits

- `f95bdcf Edit job notes at every stage and remove audit vulnerabilities (#86)` — last lockfile security refresh; it predates these current advisory bounds.
- No TASK-211 source change touches frontend dependencies.

### Instrumentation data

- `browserslist@4.28.2` arrives transitively through `autoprefixer@10.5.0`.
- `postcss-selector-parser@6.1.2` arrives transitively through `tailwindcss@3.4.19` / `postcss-nested@6.2.0`.
- `npm audit fix --dry-run` requires no direct dependency or major-version change; it updates seven compatible transitive lock entries, including browserslist 4.28.8 and postcss-selector-parser 6.1.4.

### Hypothesized root cause

The frozen lockfile predates newly published advisory-safe transitive patch releases. · Confidence: high

## Phase 2 — Pattern

This recurs when advisories are published after a previously clean lockfile was committed. The repository previously resolved the same class in `f95bdcf`. A fresh-lock audit is the check that catches it; product CV code is unrelated.

## Phase 3 — Impact

Affected file: `frontend/package-lock.json` only. These packages are CSS/browser-target build tooling. No mail, CV evidence, model, API, or runtime authorization path changes.

## Phase 4 — Solution

Run ordinary `npm audit fix` (never `--force`), retain the existing direct dependencies, and rerun frontend tests, build, and audit.

## Resolution

`npm audit fix` changed only compatible transitive lock entries. All 206 frontend tests and the TypeScript/Vite build passed; final audit reported 0 vulnerabilities.
