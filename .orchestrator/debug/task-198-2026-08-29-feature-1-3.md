# Debug session: Gmail still shows RFC822 search
Created: 2026-08-29T21:05:00Z
Session: 01a0432f-8243-7150-8255-1caacc65057c

## Phase 1 — Root Cause

### Error
User report: `this is what I see when clicking on one of the messages for formunauts, but I am not inside the conversation`

Screenshot evidence: Gmail opened `/mail/u/1/#search/rfc822msgid:...` and displayed one search result rather than the conversation.

### Reproduction
The source screenshot immediately before the click shows `http://localhost:8000/mailbox`.

Environment: Windows; port 8000 is served by PID 254988 with `C:\Users\Administrator\Backup\Projects\dachapply\.venv\Scripts\python.exe manage.py runserver 127.0.0.1:8000`. The main worktree is on `wave9-auto-interview-date` at `cec0f18`, where `gmail_conversation_url(message_id, authuser='')` still always emits the RFC822 search route. The deployed/merged worktree is at `ae4f8b0`, where the builder prefers persisted `thread_id` and emits `#all/<thread_id>`.

Frequency: always when clicking a link generated by that stale local server for a message with an RFC822 ID.

### Suspect commits
- `cec0f18 docs(mailbox): explain cloud ingestion recovery` — not a regression commit; it identifies the old branch currently serving localhost.
- `ae4f8b0 fix(mailbox): open direct Gmail conversations` — contains the intended behavior but is not loaded by the localhost process.

### Instrumentation data
- Screenshot source URL: `localhost:8000/mailbox`.
- Screenshot destination URL: `mail.google.com/mail/u/1/#search/rfc822msgid:...`.
- Local running source signature: two-argument builder with unconditional `#search/rfc822msgid:` output.
- Merged source signature: four-argument builder with `if thread_id: ... #all/<thread_id>`.
- Public deployment workflow for `ae4f8b0` completed successfully.

### Hypothesized root cause
The user clicked a URL generated by the stale localhost server running the unrelated pre-fix worktree, not the deployed `ae4f8b0` application. · Confidence: high

## Phase 2 — Pattern

This is an environment/version mismatch, not recurrence in the merged implementation. The direct-thread and fallback branches already have automated builder and serializer coverage. The visible `localhost:8000` source URL is the missing environment signal.

## Phase 3 — Impact

No production file requires a change. The mismatch affects links generated by the currently running local development process only. Restarting or altering that process risks disturbing the unrelated dirty `wave9-auto-interview-date` worktree, so it must remain untouched.

## Phase 4 — Solution

Repeat the verification from `https://dachapply.livelysea-3461ad21.westeurope.azurecontainerapps.io/mailbox`, confirming that address before clicking the message's arrow. Do not use or restart the unrelated localhost server for this check.

## Resolution

No code change required. Environment mismatch confirmed from both screenshots, process metadata, active branch, and source comparison.
