# Metrics

- `learnings.jsonl` — session-scoped learnings managed by `/evolve`.
- `sessions.jsonl` — per-session execution summaries written by `/close`.

Schema: https://gitlab.gotzendorfer.at/infrastructure/session-orchestrator

**Do not gitignore these files.** They persist project history across sessions.
